# Normalise text files to LF in this repo (cross-platform friendly)
* text=auto eol=lf

# Common documentation / configs
*.md   text eol=lf
*.yml  text eol=lf
*.yaml text eol=lf
*.txt  text eol=lf

# Scripts / code
*.py   text eol=lf
*.sh   text eol=lf

# PowerShell scripts: keep CRLF for best Windows UX (optional but sensible)
*.ps1  text eol=crlf
